language = "java17"

compile = "javac -classpath .:target/* -d . $(find . -type f -name '*.java')"
run = ["java", "-classpath", ".:target/*", "BookshelfApplication"]

entrypoint = "BookshelfApplication.java"
hidden = ["**/*.class"]

[nix]
channel = "stable-21_11"
